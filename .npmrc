; pi provides @earendil-works/* (and the @mariozechner/* aliases) at runtime
; via jiti, so the declared peer dependency must NOT be auto-installed for
; local dev/test — doing so pulls the entire pi runtime (AI SDKs, native
; bindings) into node_modules. Keep dev installs limited to devDependencies.
legacy-peer-deps=true
